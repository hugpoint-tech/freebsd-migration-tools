{
 "id": 1216425,
 "bug_id": 264779,
 "attachment_id": 0,
 "creation_time": "2022-06-20T02:39:44Z",
 "creator": "freebsd@winter.cafe",
 "text": "After both setting custom name servers and enabling local_unbound during install, local_unbound's setup script does not use the name servers I set to generate the forwarder configuration. However, the /etc/resolv.conf.bak file that the setup script created contains the correct name servers, so I have no clue where exactly it got the incorrect name servers from.\n\nTo reproduce:\n1. Set custom name servers during bsdinstall, as well as enable local_unbound\n2. Observe that incorrect name servers are specified in /etc/unbound/forward.conf\n\nSomething peculiar that I think is worth noting is that the generated forward configuration contains \"# Generated by resolvconf\", and local_unbound's setup script creates a resolvconf configuration file. So it looks like these two processes are clashing, since resolvconf overrides the forward configuration that local_unbound's setup script generates."
}