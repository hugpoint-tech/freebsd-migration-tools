{
 "id": 897070,
 "bug_id": 212538,
 "attachment_id": 0,
 "creation_time": "2016-09-09T20:32:59Z",
 "creator": "wollman@FreeBSD.org",
 "text": "The standard way for handling account expiration when using LDAP for \"password database\" purposes is to define a shadowExpire attribute on the user.  On Linux and Solaris, this is implemented by the goofy bag-on-the-side \"shadow\" mechanism, which is treated as a separate database in NSS, and the architecture of nss-pam-ldapd reflects this.  Account expiration in FreeBSD is implemented in the standard password database, but the nslcd stubs in nss-pam-ldapd's nsswitch module do not do the extra RPC to look up the expiration information via the \"shadow\" stuff and merge it into the passwd entry.\n\nAs a result, you cannot use nss-pam-ldapd in a FreeBSD environment if your directory operator uses shadowExpire to disable logins."
}