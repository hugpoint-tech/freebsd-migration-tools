{
 "id": 1211828,
 "bug_id": 263675,
 "attachment_id": 0,
 "creation_time": "2022-05-19T10:40:35Z",
 "creator": "se@FreeBSD.org",
 "text": "No, libexec is not for shared libraries, it is the place for binaries that are not typically started from an interactive shell (but rather as a service, from inetd, or invoked as a sub-process that is not run on its own).\n\nThe major difference is that commands in libexec are not in any shell path by default, you have to invoke them with a full path (or temporarily add libexec to the PATH in the current process environment).\n\nSince this is a command that is waiting for requests as a background task started as a service via etc/rc.d, I'd think that it should be placed in libexec and the full path should be patched into the rc file.\n\nThis will not be necessary for a command in sbin, but then it will clutter the list of commands presented to an administrator that uses name completion in a shell, for example. And I'd try to avoid that for commands that are not used in interactive shells."
}